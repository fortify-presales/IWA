    public static class WebClass {
        private int id;

        public WebClass(int id) {
            this.id = id;
        }

        public void process() {
        }
    }
    private WebClass innerClass;

    private void nullPointer(int id) {
        WebClass w;
        w = null;
        if (id > 0) {
            w = new WebClass(id);
        }
        w.process();
    }
